

class {{model.name}}(dict):

    def __init__(self, {{_.lowerCase model.name}}):
        self.update({{_.lowerCase model.name}})

{{#each model.fields}}
    @property
    {{this.name}}
    def {{this.name}}(self):
        """
        {{this.doc.description}}

        :return: {{this.doc.returns}}
        """
        return self.get('{{this.name}}')
{{/each}}

    {{>model-overrides}}